FROM m.daocloud.io/docker.io/library/node:20

RUN apt-get update && apt-get install -y git bash && rm -rf /var/lib/apt/lists/*

WORKDIR /app

RUN npm install -g opencode-ai@latest

RUN useradd -m sleep0

USER sleep0