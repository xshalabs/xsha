# Sleep0 Backend 环境配置文件
# 将此文件复制为 .env 并根据需要修改配置值
# 注意：环境变量的优先级高于 .env 文件中的配置

# ========== 基础配置 ==========
# 服务端口
SLEEP0_PORT=8080

# 运行环境 (development/production)
SLEEP0_ENVIRONMENT=development

# ========== 数据库配置 ==========
# 数据库类型 (sqlite/mysql)
SLEEP0_DATABASE_TYPE=sqlite

# SQLite 数据库文件路径
SLEEP0_SQLITE_PATH=app.db

# MySQL 连接字符串（当使用 MySQL 时）
# SLEEP0_MYSQL_DSN=user:password@tcp(localhost:3306)/dbname?charset=utf8mb4&parseTime=True&loc=Local

# ========== 认证与安全配置 ==========
# 管理员用户名
SLEEP0_ADMIN_USER=admin

# 管理员密码（建议使用加密工具加密后的值）
SLEEP0_ADMIN_PASS=admin123

# JWT 签名密钥（生产环境请更换为复杂密钥）
SLEEP0_JWT_SECRET=your-jwt-secret-key-change-this-in-production

# AES 加密密钥（用于密码加密，生产环境请更换）
SLEEP0_AES_KEY=default-aes-key-change-in-production

# ========== 定时器配置 ==========
# 定时器执行间隔
SLEEP0_SCHEDULER_INTERVAL=30s

# 工作目录基础路径
SLEEP0_WORKSPACE_BASE_DIR=/tmp/sleep0-workspaces

# Docker 执行超时时间
SLEEP0_DOCKER_TIMEOUT=30m

# 最大并发任务数
SLEEP0_MAX_CONCURRENT_TASKS=5

# ========== Git 配置 ==========
# Git SSL 验证开关 (true/false)
SLEEP0_GIT_SSL_VERIFY=false

# ========== 日志配置 ==========
# 日志级别 (DEBUG/INFO/WARN/ERROR)
SLEEP0_LOG_LEVEL=INFO

# 日志格式 (JSON/TEXT)
SLEEP0_LOG_FORMAT=JSON

# 日志输出 (stdout/stderr/文件路径)
SLEEP0_LOG_OUTPUT=stdout 